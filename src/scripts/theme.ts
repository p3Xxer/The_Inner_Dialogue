// Light/dark toggle removed — site is dark only.
// Reading mode (terminal/typewriter) is handled via data-reading-mode attribute on <html>.
// See src/theme.ts for the design system color values and reading mode overrides.
